{{ 'section-service-panel.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign panel_heading = section.settings.heading
  assign panel_text = section.settings.text
  assign panel_button = section.settings.button_label

  if request.locale.iso_code != 'en'
    if page.handle == 'about' and panel_heading == 'Discover the current selection.'
      assign panel_heading = 'templates.page_cta.about_heading' | t
      assign panel_text = 'templates.page_cta.about_text' | t
      assign panel_button = 'templates.page_cta.about_button' | t
    elsif page.handle == 'authenticity' and panel_heading == 'Understanding condition'
      assign panel_heading = 'templates.page_cta.authenticity_heading' | t
      assign panel_text = 'templates.page_cta.authenticity_text' | t
      assign panel_button = 'templates.page_cta.authenticity_button' | t
    endif
  endif
-%}

<div class="service-panel color-{{ section.settings.color_scheme }} gradient">
  <div class="page-width">
    <div class="service-panel__inner{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
      {%- if section.settings.eyebrow != blank -%}
        <p class="service-panel__eyebrow caption-with-letter-spacing">{{ section.settings.eyebrow | escape }}</p>
      {%- endif -%}
      {%- if panel_heading != blank -%}
        <h2 class="service-panel__heading inline-richtext {{ section.settings.heading_size }}">
          {{- panel_heading -}}
        </h2>
      {%- endif -%}
      {%- if panel_text != blank -%}
        <div class="service-panel__text rte">{{ panel_text }}</div>
      {%- endif -%}
      <div class="service-panel__actions">
        {%- if panel_button != blank -%}
          <a
            {% if section.settings.button_link == blank %}
              role="link"
              aria-disabled="true"
            {% else %}
              href="{{ section.settings.button_link }}"
            {% endif %}
            class="button"
          >
            {{- panel_button | escape -}}
          </a>
        {%- endif -%}
        {%- if section.settings.link_label != blank and section.settings.link_url != blank -%}
          <a href="{{ section.settings.link_url }}" class="link underlined-link">
            {{- section.settings.link_label | escape -}}
          </a>
        {%- endif -%}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Service panel",
  "tag": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Personal sourcing"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "Looking for a particular piece?"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "Heading size",
      "options": [
        { "value": "h2", "label": "Small" },
        { "value": "h1", "label": "Medium" }
      ],
      "default": "h1"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text",
      "default": "<p>Tell us the house, model, colour or era you have in mind. We will keep your request in view as we source new pieces.</p>"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Start a search"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "text",
      "id": "link_label",
      "label": "Text link label"
    },
    {
      "type": "url",
      "id": "link_url",
      "label": "Text link"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-2"
    }
  ],
  "presets": [
    {
      "name": "Service panel"
    }
  ]
}
{% endschema %}
